name: Flutter Build Bot

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Tipe build: apk-debug | apk-release | aab-release'
        required: false
        default: 'apk-release'
        type: choice
        options:
          - apk-debug
          - apk-release
          - aab-release
      flutter_version:
        description: 'Flutter version override (kosongkan untuk default)'
        required: false
        default: ''
        type: string
      release_tag:
        description: 'Release tag dari bot Telegram'
        required: false
        default: ''
        type: string

  push:
    branches:
      - main
    paths:
      - 'lib/**'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'android/**'
      - 'assets/**'

jobs:
  build:
    name: Build Flutter App
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ============================================
      # ⬇️ DOWNLOAD ZIP DARI RELEASE BOT
      # ============================================
      - name: 📥 Download Project ZIP from Bot Release
        if: github.event.inputs.release_tag != ''
        run: |
          echo "📥 Mendownload project dari release: ${{ github.event.inputs.release_tag }}"
          
          echo "⏳ Menunggu 15 detik untuk memastikan release siap..."
          sleep 15
          
          MAX_RETRY=10
          RETRY=0
          DOWNLOAD_SUCCESS=0
          
          while [ $RETRY -lt $MAX_RETRY ] && [ $DOWNLOAD_SUCCESS -eq 0 ]; do
            echo "🔍 Attempt $((RETRY+1))/$MAX_RETRY: Mencari release..."
            
            RELEASE_RESPONSE=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ github.event.inputs.release_tag }}")
            
            RELEASE_ID=$(echo "$RELEASE_RESPONSE" | jq -r '.id')
            
            if [ "$RELEASE_ID" != "null" ] && [ -n "$RELEASE_ID" ]; then
              echo "✅ Release ditemukan! ID: $RELEASE_ID"
              
              ASSET_ID=$(echo "$RELEASE_RESPONSE" | jq -r '.assets[0].id')
              ASSET_URL=$(echo "$RELEASE_RESPONSE" | jq -r '.assets[0].url')
              BROWSER_URL=$(echo "$RELEASE_RESPONSE" | jq -r '.assets[0].browser_download_url')
              ASSET_NAME=$(echo "$RELEASE_RESPONSE" | jq -r '.assets[0].name')
              ASSET_SIZE=$(echo "$RELEASE_RESPONSE" | jq -r '.assets[0].size')
              
              echo "Asset ID: $ASSET_ID"
              echo "Asset Name: $ASSET_NAME"
              echo "Asset Size: $ASSET_SIZE bytes"
              
              if [ "$ASSET_ID" != "null" ] && [ -n "$ASSET_ID" ]; then
                echo "📦 Download via API: $ASSET_URL"
                curl -L -f -H "Accept: application/octet-stream" \
                  -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                  -o project.zip \
                  "$ASSET_URL"
                
                CURL_EXIT=$?
                FILE_SIZE=$(stat -c%s project.zip 2>/dev/null || echo 0)
                
                if [ $CURL_EXIT -eq 0 ] && [ "$FILE_SIZE" -gt 1000 ]; then
                  echo "✅ Download sukses! Size: $FILE_SIZE bytes"
                  DOWNLOAD_SUCCESS=1
                  break
                else
                  echo "⚠️ API download gagal, coba browser URL..."
                  curl -L -f -o project.zip "$BROWSER_URL"
                  CURL_EXIT=$?
                  FILE_SIZE=$(stat -c%s project.zip 2>/dev/null || echo 0)
                  
                  if [ $CURL_EXIT -eq 0 ] && [ "$FILE_SIZE" -gt 1000 ]; then
                    echo "✅ Browser download sukses! Size: $FILE_SIZE bytes"
                    DOWNLOAD_SUCCESS=1
                    break
                  fi
                fi
              fi
            fi
            
            echo "⚠️ Download gagal, tunggu 20 detik..."
            sleep 20
            RETRY=$((RETRY+1))
          done
          
          if [ $DOWNLOAD_SUCCESS -eq 0 ]; then
            echo "❌ Gagal download setelah $MAX_RETRY kali percobaan!"
            echo ""
            echo "📋 Daftar release yang ada:"
            curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/releases?per_page=5" | \
              jq '.[] | {tag_name, assets: [.assets[]? | {name, size, state}]}'
            exit 1
          fi
          
          echo "📏 Final size: $(du -h project.zip | cut -f1)"
          
          echo "📂 Mengekstrak ZIP..."
          mkdir -p project_extracted
          unzip -o project.zip -d project_extracted/ || {
            echo "❌ unzip gagal, mencoba 7z..."
            sudo apt-get install -y -qq p7zip-full 2>/dev/null
            7z x project.zip -oproject_extracted/ -y || {
              echo "❌ Ekstrak gagal total!"
              echo "File type: $(file project.zip)"
              exit 1
            }
          }
          
          if [ -f "project_extracted/pubspec.yaml" ]; then
            echo "✅ pubspec.yaml ditemukan di root ZIP"
            cp -rf project_extracted/* .
            cp -rf project_extracted/.* . 2>/dev/null || true
          else
            echo "🔍 Mencari pubspec.yaml di subfolder..."
            SUBFOLDER=$(find project_extracted -name "pubspec.yaml" -not -path "*/.*" | head -1)
            
            if [ -n "$SUBFOLDER" ]; then
              PROJECT_DIR=$(dirname "$SUBFOLDER")
              echo "✅ pubspec.yaml ditemukan di: $PROJECT_DIR"
              cp -rf "$PROJECT_DIR"/* .
              cp -rf "$PROJECT_DIR"/.* . 2>/dev/null || true
            else
              echo "❌ pubspec.yaml TIDAK DITEMUKAN!"
              echo "Isi ZIP:"
              ls -laR project_extracted/ | head -50
              exit 1
            fi
          fi
          
          rm -rf project_extracted project.zip
          
          echo "✅ Project siap:"
          ls -la
          head -20 pubspec.yaml

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'
          channel: 'stable'
          cache: true

      # ============================================
      # 🎨 REGENERATE LAUNCHER ICONS
      # ============================================
      - name: 🎨 Regenerate Launcher Icons
        run: |
          echo "=========================================="
          echo "=== REGENERATING LAUNCHER ICONS ==="
          echo "=========================================="
          
          if ! command -v convert >/dev/null 2>&1; then
            echo "📦 Installing ImageMagick..."
            sudo apt-get update -qq && sudo apt-get install -y -qq imagemagick
          fi
          
          SOURCE_ICON=""
          if [ -f "assets/icon/icon.jpg" ]; then
            SOURCE_ICON="assets/icon/icon.jpg"
          elif [ -f "assets/icon/icon.png" ]; then
            SOURCE_ICON="assets/icon/icon.png"
          elif [ -f "assets/icon/icon.webp" ]; then
            SOURCE_ICON="assets/icon/icon.webp"
          fi
          
          if [ -n "$SOURCE_ICON" ]; then
            echo "✅ Source icon found: $SOURCE_ICON"
            
            echo "🗑️ Removing all existing ic_launcher files..."
            find android/app/src/main/res -name "ic_launcher*" -type f -delete 2>/dev/null || true
            
            echo "🎨 Creating icons for all densities..."
            
            mkdir -p android/app/src/main/res/mipmap-mdpi
            convert "$SOURCE_ICON" -resize 48x48 -gravity center -extent 48x48 android/app/src/main/res/mipmap-mdpi/ic_launcher.png
            echo "  ✅ mipmap-mdpi/ic_launcher.png (48x48)"
            
            mkdir -p android/app/src/main/res/mipmap-hdpi
            convert "$SOURCE_ICON" -resize 72x72 -gravity center -extent 72x72 android/app/src/main/res/mipmap-hdpi/ic_launcher.png
            echo "  ✅ mipmap-hdpi/ic_launcher.png (72x72)"
            
            mkdir -p android/app/src/main/res/mipmap-xhdpi
            convert "$SOURCE_ICON" -resize 96x96 -gravity center -extent 96x96 android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
            echo "  ✅ mipmap-xhdpi/ic_launcher.png (96x96)"
            
            mkdir -p android/app/src/main/res/mipmap-xxhdpi
            convert "$SOURCE_ICON" -resize 144x144 -gravity center -extent 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
            echo "  ✅ mipmap-xxhdpi/ic_launcher.png (144x144)"
            
            mkdir -p android/app/src/main/res/mipmap-xxxhdpi
            convert "$SOURCE_ICON" -resize 192x192 -gravity center -extent 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
            echo "  ✅ mipmap-xxxhdpi/ic_launcher.png (192x192)"
            
            echo "🎨 Creating adaptive icon (Android 8+)..."
            mkdir -p android/app/src/main/res/mipmap-anydpi-v26
            mkdir -p android/app/src/main/res/drawable
            convert "$SOURCE_ICON" -resize 108x108 -gravity center -extent 108x108 android/app/src/main/res/mipmap-xxhdpi/ic_launcher_background.png
            convert "$SOURCE_ICON" -resize 72x72 -gravity center -extent 108x108 android/app/src/main/res/mipmap-xxhdpi/ic_launcher_foreground.png
            
            cat > android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml << 'XMLEOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@mipmap/ic_launcher_background"/>
              <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
          </adaptive-icon>
          XMLEOF
            echo "  ✅ mipmap-anydpi-v26/ic_launcher.xml"
            
          else
            echo "⚠️ No source icon found, creating placeholder icons..."
            
            for density in mdpi hdpi xhdpi xxhdpi xxxhdpi; do
              case $density in
                mdpi) SIZE="48x48" ;;
                hdpi) SIZE="72x72" ;;
                xhdpi) SIZE="96x96" ;;
                xxhdpi) SIZE="144x144" ;;
                xxxhdpi) SIZE="192x192" ;;
              esac
              
              OUTPUT_DIR="android/app/src/main/res/mipmap-${density}"
              mkdir -p "$OUTPUT_DIR"
              
              convert -size "$SIZE" xc:'#2196F3' \
                -fill white -gravity center -pointsize 24 \
                -annotate 0 "App" \
                "$OUTPUT_DIR/ic_launcher.png"
              
              echo "  ✅ Placeholder: mipmap-${density}/ic_launcher.png (${SIZE})"
            done
          fi
          
          echo ""
          echo "=== ICON REGENERATION COMPLETE ==="
          echo "Files created:"
          find android/app/src/main/res -name "ic_launcher*" -type f | sort

      # ============================================
      # 🧹 CLEAN ICON RESOURCES
      # ============================================
      - name: 🧹 Clean Icon Resources
        run: |
          echo "=========================================="
          echo "=== CLEANING ICON RESOURCES ==="
          echo "=========================================="
          
          echo "🗑️ Removing non-PNG icon files..."
          find android/app/src/main/res -type f \( \
            -name "ic_launcher.jpg" -o \
            -name "ic_launcher.webp" -o \
            -name "ic_launcher.gif" -o \
            -name "ic_launcher.bmp" -o \
            -name "ic_launcher_round.jpg" -o \
            -name "ic_launcher_round.webp" -o \
            -name "ic_launcher_round.gif" -o \
            -name "ic_launcher_foreground.jpg" -o \
            -name "ic_launcher_foreground.webp" -o \
            -name "ic_launcher_background.jpg" -o \
            -name "ic_launcher_background.webp" \
          \) -delete 2>/dev/null || true
          
          echo "🗑️ Removing 0-byte icon files..."
          find android/app/src/main/res -name "ic_launcher*" -type f -size 0 -delete 2>/dev/null || true
          
          echo ""
          echo "=== REMAINING ICON FILES ==="
          find android/app/src/main/res -name "ic_launcher*" -type f -exec ls -lh {} \; | sort
          
          echo ""
          echo "✅ Icon cleanup complete"

      # ============================================
      # 🔍 VALIDATE RESOURCES
      # ============================================
      - name: 🔍 Validate Android Resources
        run: |
          echo "=========================================="
          echo "=== VALIDATING ANDROID RESOURCES ==="
          echo "=========================================="
          
          ERROR_FOUND=0
          
          for png in $(find android/app/src/main/res -name "*.png" -type f); do
            SIZE=$(stat -c%s "$png" 2>/dev/null || echo 0)
            if [ "$SIZE" -eq 0 ]; then
              echo "❌ EMPTY FILE: $png (0 bytes)"
              ERROR_FOUND=1
              rm -f "$png"
              continue
            fi
            
            if command -v identify >/dev/null 2>&1; then
              if identify "$png" >/dev/null 2>&1; then
                DIMENSIONS=$(identify -format "%wx%h" "$png" 2>/dev/null)
                echo "  ✅ $png ($DIMENSIONS, ${SIZE} bytes)"
              else
                echo "❌ CORRUPT PNG: $png"
                ERROR_FOUND=1
                
                echo "  🔧 Attempting to fix..."
                convert -size 48x48 xc:'#2196F3' "$png" 2>/dev/null || {
                  echo "  ❌ Cannot fix, removing..."
                  rm -f "$png"
                }
              fi
            fi
          done
          
          if [ $ERROR_FOUND -eq 1 ]; then
            echo ""
            echo "⚠️ Some resources were corrupt and have been fixed/removed"
          else
            echo ""
            echo "✅ All PNG resources are valid!"
          fi
          
          echo ""
          echo "=== FINAL CHECK: Required icons ==="
          for density in mdpi hdpi xhdpi xxhdpi xxxhdpi; do
            ICON_PATH="android/app/src/main/res/mipmap-${density}/ic_launcher.png"
            if [ -f "$ICON_PATH" ]; then
              SIZE=$(stat -c%s "$ICON_PATH")
              echo "  ✅ mipmap-${density}/ic_launcher.png ($SIZE bytes)"
            else
              echo "  ❌ MISSING: mipmap-${density}/ic_launcher.png"
              ERROR_FOUND=1
            fi
          done
          
          if [ $ERROR_FOUND -eq 1 ]; then
            echo ""
            echo "❌ VALIDATION FAILED - Some required icons are missing!"
          else
            echo ""
            echo "✅ VALIDATION PASSED - All resources are ready!"
          fi

      # PERBAIKAN: Fix Java, Namespace, Kotlin, minSdk, compileSdk
      - name: Fix Java and Namespace
        run: |
          FILE_PATH="android/app/src/main/java/com/nullx/pp/SpyService.java"
          if [ -f "$FILE_PATH" ]; then
            sed -i '/import com.nullx.pp.R;/d' "$FILE_PATH"
            echo "✅ Import R dihapus"
          fi
          
          BUILD_GRADLE="android/app/build.gradle"
          if [ -f "$BUILD_GRADLE" ]; then
            if ! grep -q "namespace 'com.nullx.pp'" "$BUILD_GRADLE"; then
              sed -i "/android {/a \    namespace 'com.nullx.pp'" "$BUILD_GRADLE"
              echo "✅ Namespace dipaksa ke com.nullx.pp"
            fi
          fi

      - name: Fix Kotlin Version Compatibility
        run: |
          BUILD_GRADLE="android/build.gradle"
          if [ -f "$BUILD_GRADLE" ]; then
            if grep -q "kotlin_version" "$BUILD_GRADLE"; then
              sed -i "s/ext\.kotlin_version = '[^']*'/ext.kotlin_version = '1.9.10'/" "$BUILD_GRADLE"
              echo "kotlin_version patched ke 1.9.10"
            else
              sed -i "/buildscript {/a\\    ext.kotlin_version = '1.9.10'" "$BUILD_GRADLE"
              echo "kotlin_version ditambahkan"
            fi
          fi
          echo "--- android/build.gradle ---"
          cat "$BUILD_GRADLE" 2>/dev/null || echo "(not found)"

      - name: 🔧 Fix minSdkVersion & AndroidManifest
        run: |
          echo "=== Fixing all gradle files ==="
          
          find android -name "build.gradle" -o -name "build.gradle.kts" | while read f; do
            echo "Processing: $f"
            sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' "$f"
            sed -i 's/minSdkVersion 21/minSdkVersion 23/g' "$f"
            sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 23/g' "$f"
            sed -i 's/minSdk(flutter.minSdkVersion)/minSdk(23)/g' "$f"
            sed -i 's/minSdk = 21/minSdk = 23/g' "$f"
            sed -i 's/minSdk 21/minSdk 23/g' "$f"
          done
          
          echo "=== All gradle files fixed ==="
          grep -r "minSdk" android/ 2>/dev/null | head -10
          
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            echo "=== Fixing AndroidManifest ==="
            
            if ! grep -q "xmlns:tools" "$MANIFEST"; then
              sed -i 's/<manifest/<manifest xmlns:tools="http:\/\/schemas.android.com\/tools"/' "$MANIFEST"
              echo "✅ xmlns:tools added"
            fi
            
            if ! grep -q "overrideLibrary" "$MANIFEST"; then
              sed -i 's/<application/<application\n        tools:overrideLibrary="com.llfbandit.record"/' "$MANIFEST"
              echo "✅ overrideLibrary added"
            fi
            
            echo "--- AndroidManifest.xml ---"
            head -12 "$MANIFEST"
          fi

      - name: 🔧 Fix compileSdk, Assets & Java
        run: |
          echo "=========================================="
          echo "=== FIX 1: compileSdk ke 36 ==="
          echo "=========================================="
          
          find android -type f \( -name "build.gradle" -o -name "build.gradle.kts" \) | while read f; do
            echo "=== Before: $f ==="
            grep -n "compileSdk" "$f" || echo "(no compileSdk found)"
            
            sed -i 's/compileSdkVersion\s*=\?\s*35/compileSdkVersion 36/g' "$f"
            sed -i 's/compileSdkVersion\s*=\?\s*34/compileSdkVersion 36/g' "$f"
            sed -i 's/compileSdkVersion\s*=\?\s*33/compileSdkVersion 36/g' "$f"
            sed -i 's/compileSdk\s*=\?\s*35/compileSdk = 36/g' "$f"
            sed -i 's/compileSdk\s*=\?\s*34/compileSdk = 36/g' "$f"
            sed -i 's/compileSdk\s*=\?\s*33/compileSdk = 36/g' "$f"
            sed -i 's/compileSdk(35)/compileSdk(36)/g' "$f"
            sed -i 's/compileSdk(34)/compileSdk(36)/g' "$f"
            sed -i 's/compileSdk(33)/compileSdk(36)/g' "$f"
            sed -i 's/compileSdk\s\+35/compileSdk 36/g' "$f"
            sed -i 's/compileSdk\s\+34/compileSdk 36/g' "$f"
            
            echo "=== After: $f ==="
            grep -n "compileSdk" "$f" || echo "(no compileSdk)"
          done
          
          if [ -f android/build.gradle ]; then
            sed -i 's/compileSdkVersion\s*=\?\s*35/compileSdkVersion 36/g' android/build.gradle
            sed -i 's/compileSdk\s*=\?\s*35/compileSdk = 36/g' android/build.gradle
          fi

          echo ""
          echo "=========================================="
          echo "=== FIX 2: Buat folder assets ==="
          echo "=========================================="
          
          for dir in assets assets/images assets/videos assets/audio assets/fonts assets/icons; do
            if [ ! -d "$dir" ]; then
              mkdir -p "$dir"
              touch "$dir/.gitkeep"
              echo "✅ Created: $dir/"
            else
              echo "✅ Exists: $dir/"
            fi
          done
          
          if [ -f pubspec.yaml ]; then
            echo "Reading pubspec.yaml for assets..."
            grep -E "^\s+-\s+" pubspec.yaml | sed 's/.*- //' | while read asset; do
              if echo "$asset" | grep -q "/$"; then
                if [ ! -d "$asset" ]; then
                  mkdir -p "$asset"
                  touch "$asset/.gitkeep"
                  echo "✅ Created from pubspec: $asset"
                fi
              else
                PARENT_DIR=$(dirname "$asset")
                if [ "$PARENT_DIR" != "." ] && [ ! -d "$PARENT_DIR" ]; then
                  mkdir -p "$PARENT_DIR"
                  touch "$PARENT_DIR/.gitkeep"
                  echo "✅ Created parent: $PARENT_DIR/"
                fi
                if [ ! -f "$asset" ]; then
                  touch "$asset"
                  echo "✅ Created placeholder: $asset"
                fi
              fi
            done
          fi

          echo ""
          echo "=========================================="
          echo "=== FIX 3: Java InputType ==="
          echo "=========================================="
          
          JAVA_FILE="android/app/src/main/java/com/nullx/pp/LockOverlayService.java"
          if [ -f "$JAVA_FILE" ]; then
            echo "Fixing: $JAVA_FILE"
            cp "$JAVA_FILE" "$JAVA_FILE.bak"
            
            sed -i 's/InputType\.TYPE_NUMBER_PASSWORD/(InputType.TYPE_CLASS_NUMBER | InputType.TYPE_NUMBER_VARIATION_PASSWORD)/g' "$JAVA_FILE"
            
            if grep -q "TYPE_NUMBER_PASSWORD" "$JAVA_FILE"; then
              echo "❌ Fix failed, masih ada TYPE_NUMBER_PASSWORD"
              if ! grep -q "import android.text.InputType" "$JAVA_FILE"; then
                sed -i '1s/^/import android.text.InputType;\n/' "$JAVA_FILE"
              fi
              sed -i 's/InputType\.TYPE_NUMBER_PASSWORD/129/g' "$JAVA_FILE"
              echo "✅ Fixed with numeric value 129"
            else
              echo "✅ TYPE_NUMBER_PASSWORD fixed"
            fi
          else
            echo "⚠️ LockOverlayService.java not found, searching..."
            find android -name "*.java" -type f | while read f; do
              if grep -l "TYPE_NUMBER_PASSWORD" "$f" 2>/dev/null; then
                echo "Found in: $f"
                sed -i 's/InputType\.TYPE_NUMBER_PASSWORD/(InputType.TYPE_CLASS_NUMBER | InputType.TYPE_NUMBER_VARIATION_PASSWORD)/g' "$f"
                echo "✅ Fixed: $f"
              fi
            done
          fi

          echo ""
          echo "=== ALL FIXES COMPLETE ==="
          echo ""
          echo "=== Verification ==="
          echo "compileSdk values:"
          grep -rn "compileSdk" android/ 2>/dev/null | grep -v ".bak" | head -10
          echo ""
          echo "Asset directories:"
          ls -d assets/*/ 2>/dev/null || echo "(no asset dirs)"
          echo ""
          echo "TYPE_NUMBER_PASSWORD occurrences:"
          grep -rn "TYPE_NUMBER_PASSWORD" android/ 2>/dev/null || echo "✅ None found (FIXED!)"

      - name: Set Build Type
        id: build_info
        run: |
          BUILD_TYPE="${{ github.event.inputs.build_type }}"
          if [ -z "$BUILD_TYPE" ]; then
            BUILD_TYPE="apk-debug"
          fi
          echo "build_type=$BUILD_TYPE" >> $GITHUB_OUTPUT
          echo "Build type: $BUILD_TYPE"

      - name: Flutter Pub Get
        id: pub_get
        continue-on-error: true
        run: |
          flutter config --no-analytics
          dart --disable-analytics
          flutter pub get

      # ============================================
      # 🏗️ BUILD APK DEBUG (DENGAN VERIFIKASI)
      # ============================================
      - name: Build APK Debug
        id: build_debug
        if: steps.build_info.outputs.build_type == 'apk-debug'
        continue-on-error: true
        run: |
          echo "=== MULAI BUILD APK DEBUG ==="
          flutter build apk --debug \
            --no-shrink \
            --target-platform android-arm,android-arm64,android-x64 2>&1 | tee build_log.txt
          BUILD_EXIT=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_EXIT"
          
          echo ""
          echo "=== MENCARI FILE HASIL BUILD ==="
          find . -name "*.apk" -type f 2>/dev/null
          find . -name "*.aab" -type f 2>/dev/null
          echo ""
          echo "=== ISI build/app/outputs/ ==="
          ls -laR build/app/outputs/ 2>/dev/null || echo "(tidak ditemukan)"
          echo ""
          
          if [ -f build/app/outputs/flutter-apk/app-debug.apk ]; then
            echo "✅ app-debug.apk FOUND!"
            ls -lh build/app/outputs/flutter-apk/app-debug.apk
          else
            echo "❌ app-debug.apk NOT FOUND!"
          fi
          
          exit $BUILD_EXIT

      # ============================================
      # 🏗️ BUILD APK RELEASE (DENGAN VERIFIKASI)
      # ============================================
      - name: Build APK Release
        id: build_release
        if: steps.build_info.outputs.build_type == 'apk-release'
        continue-on-error: true
        run: |
          echo "=== MULAI BUILD APK RELEASE ==="
          flutter build apk --release \
            --obfuscate \
            --split-debug-info=build/debug-info \
            --target-platform android-arm,android-arm64,android-x64 2>&1 | tee build_log.txt
          BUILD_EXIT=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_EXIT"
          
          echo ""
          echo "=== MENCARI FILE HASIL BUILD ==="
          find . -name "*.apk" -type f 2>/dev/null
          find . -name "*.aab" -type f 2>/dev/null
          echo ""
          echo "=== ISI build/app/outputs/ ==="
          ls -laR build/app/outputs/ 2>/dev/null || echo "(tidak ditemukan)"
          echo ""
          
          if [ -f build/app/outputs/flutter-apk/app-release.apk ]; then
            echo "✅ app-release.apk FOUND!"
            ls -lh build/app/outputs/flutter-apk/app-release.apk
          else
            echo "❌ app-release.apk NOT FOUND!"
          fi
          
          exit $BUILD_EXIT

      # ============================================
      # 🏗️ BUILD AAB RELEASE (DENGAN VERIFIKASI)
      # ============================================
      - name: Build AAB Release
        id: build_aab
        if: steps.build_info.outputs.build_type == 'aab-release'
        continue-on-error: true
        run: |
          echo "=== MULAI BUILD AAB RELEASE ==="
          flutter build appbundle --release \
            --obfuscate \
            --split-debug-info=build/debug-info 2>&1 | tee build_log.txt
          BUILD_EXIT=${PIPESTATUS[0]}
          echo "Build exit code: $BUILD_EXIT"
          
          echo ""
          echo "=== MENCARI FILE HASIL BUILD ==="
          find . -name "*.apk" -type f 2>/dev/null
          find . -name "*.aab" -type f 2>/dev/null
          echo ""
          echo "=== ISI build/app/outputs/ ==="
          ls -laR build/app/outputs/ 2>/dev/null || echo "(tidak ditemukan)"
          echo ""
          
          if [ -f build/app/outputs/bundle/release/app-release.aab ]; then
            echo "✅ app-release.aab FOUND!"
            ls -lh build/app/outputs/bundle/release/app-release.aab
          else
            echo "❌ app-release.aab NOT FOUND!"
          fi
          
          exit $BUILD_EXIT

      # ============================================
      # 🛠️ AUTO FIX PUBSPEC & DART ERRORS
      # ============================================
      - name: 🛠️ Auto Fix Pubspec & Dart Errors
        if: failure()
        run: |
          echo "=========================================="
          echo "=== AUTO FIXING PUBSPEC & DART ERRORS ==="
          echo "=========================================="
          
          if [ -f pubspec.yaml ]; then
            echo "🔧 Fixing pubspec.yaml errors..."
            cp pubspec.yaml pubspec.yaml.bak
            
            awk '!seen[$0]++' pubspec.yaml > pubspec_clean.yaml
            mv pubspec_clean.yaml pubspec.yaml
            
            sed -i 's/^    /  /g' pubspec.yaml
            sed -i 's/^\t/  /g' pubspec.yaml
            sed -i 's/\([a-zA-Z_]\):\([a-zA-Z_]\)/\1: \2/g' pubspec.yaml
            sed -i 's/^  \([a-zA-Z_]*\) :/  \1:/g' pubspec.yaml
            sed -i '/^$/N;/^\n$/D' pubspec.yaml
            
            echo "✅ pubspec.yaml fixed!"
            head -50 pubspec.yaml
          fi
          
          echo ""
          echo "🔧 Fixing Dart code errors..."
          
          if [ -d "lib" ]; then
            find lib -name "*.dart" | while read f; do
              sed -i 's/\([^;{}]\)$/\1;/g' "$f" 2>/dev/null || true
            done
            
            dart fix --apply lib/ 2>/dev/null || true
            
            find lib -name "*.dart" | while read f; do
              sed -i 's/\(String\|int\|bool\|double\) \([a-zA-Z_]*\) =/\1? \2 =/g' "$f" 2>/dev/null || true
              sed -i 's/late \(.*\)?;/late \1;/g' "$f" 2>/dev/null || true
              sed -i 's/{this\.\([a-zA-Z_]*\),/{required this.\1,/g' "$f" 2>/dev/null || true
              sed -i 's/\.withOpacity(\(.*\))/.withValues(alpha: \1)/g' "$f" 2>/dev/null || true
              if grep -q "Widget build" "$f" 2>/dev/null; then
                if ! grep -q "@override" "$f" 2>/dev/null; then
                  sed -i '/^  Widget build/i \  @override' "$f" 2>/dev/null || true
                fi
              fi
              if grep -q "^class " "$f" 2>/dev/null; then
                if ! grep -q "const " "$f" | head -1; then
                  sed -i 's/^class \([A-Z][a-zA-Z]*\) {/class \1 {\n  const \1();/' "$f" 2>/dev/null || true
                fi
              fi
              if grep -q "Widget build" "$f" 2>/dev/null; then
                if ! grep -q "return" "$f" 2>/dev/null; then
                  sed -i '/Widget build/a \    return Container();' "$f" 2>/dev/null || true
                fi
              fi
            done
            
            echo "✅ Dart code fixes applied"
          fi
          
          echo ""
          echo "🔧 Fixing Android gradle errors..."
          
          if [ -d "android" ]; then
            find android -name "*.gradle" -o -name "*.gradle.kts" | while read f; do
              sed -i 's/compileSdkVersion [0-9]*/compileSdkVersion 36/g' "$f"
              sed -i 's/compileSdk = [0-9]*/compileSdk = 36/g' "$f"
              sed -i 's/compileSdk([0-9]*)/compileSdk(36)/g' "$f"
              sed -i 's/minSdkVersion [0-9]*/minSdkVersion 23/g' "$f"
              sed -i 's/minSdk = [0-9]*/minSdk = 23/g' "$f"
              sed -i 's/targetSdkVersion [0-9]*/targetSdkVersion 36/g' "$f"
              sed -i 's/targetSdk = [0-9]*/targetSdk = 36/g' "$f"
            done
            
            find android -name "build.gradle" | while read f; do
              if ! grep -q "namespace" "$f"; then
                PACKAGE_NAME=$(grep -r "package=" android/app/src/main/AndroidManifest.xml 2>/dev/null | sed 's/.*package="\([^"]*\)".*/\1/')
                if [ -z "$PACKAGE_NAME" ]; then
                  PACKAGE_NAME="com.nullx.pp"
                fi
                sed -i "/android {/a \    namespace '$PACKAGE_NAME'" "$f"
              fi
            done
            
            if [ -f android/build.gradle ]; then
              sed -i "s/ext.kotlin_version = '[^']*'/ext.kotlin_version = '1.9.10'/" android/build.gradle
              sed -i "s/com.android.tools.build:gradle:[0-9.]*/com.android.tools.build:gradle:8.2.0/g" android/build.gradle
            fi
            
            echo "✅ Android gradle fixes applied"
          fi
          
          echo ""
          echo "🔧 Fixing AndroidManifest errors..."
          
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            if ! grep -q "xmlns:tools" "$MANIFEST"; then
              sed -i 's/<manifest/<manifest xmlns:tools="http:\/\/schemas.android.com\/tools"/' "$MANIFEST"
            fi
            if ! grep -q "usesCleartextTraffic" "$MANIFEST"; then
              sed -i 's/<application/<application\n        android:usesCleartextTraffic="true"/' "$MANIFEST"
            fi
            if ! grep -q "INTERNET" "$MANIFEST"; then
              sed -i 's/<manifest/<manifest\n    <uses-permission android:name="android.permission.INTERNET"\/>/' "$MANIFEST"
            fi
            echo "✅ AndroidManifest fixes applied"
          fi
          
          echo ""
          echo "🔧 Fixing Java/Kotlin import errors..."
          
          if [ -d "android" ]; then
            find android -name "*.java" -o -name "*.kt" | while read f; do
              sed -i '/import.*\.R;/d' "$f" 2>/dev/null || true
              sed -i '/import.*\.R$/d' "$f" 2>/dev/null || true
              sed -i 's/InputType\.TYPE_NUMBER_PASSWORD/129/g' "$f" 2>/dev/null || true
            done
            echo "✅ Java/Kotlin fixes applied"
          fi
          
          echo ""
          echo "🔧 Fixing Gradle wrapper..."
          
          if [ -f "android/gradle/wrapper/gradle-wrapper.properties" ]; then
            sed -i 's|gradle-[0-9.]*-bin.zip|gradle-8.4-bin.zip|g' android/gradle/wrapper/gradle-wrapper.properties
            echo "✅ Gradle wrapper fixed to 8.4"
          fi
          
          echo ""
          echo "=== AUTO FIX COMPLETE ==="

      # ============================================
      # 🔄 RETRY BUILD AFTER AUTO FIX (ATTEMPT 2)
      # ============================================
      - name: 🔄 Retry Build After Auto Fix (Attempt 2)
        if: failure()
        run: |
          BUILD_TYPE="${{ steps.build_info.outputs.build_type }}"
          echo "🔄 Retrying build: $BUILD_TYPE"
          
          flutter clean 2>/dev/null || true
          flutter pub get
          
          if [ "$BUILD_TYPE" = "apk-debug" ]; then
            flutter build apk --debug --no-shrink 2>&1 | tee build_log.txt
          elif [ "$BUILD_TYPE" = "apk-release" ]; then
            flutter build apk --release 2>&1 | tee build_log.txt
          elif [ "$BUILD_TYPE" = "aab-release" ]; then
            flutter build appbundle --release 2>&1 | tee build_log.txt
          fi
          
          echo ""
          echo "=== HASIL BUILD RETRY ==="
          find . -name "*.apk" -type f 2>/dev/null
          find . -name "*.aab" -type f 2>/dev/null

      # ============================================
      # 🛠️ DEEP FIX & FINAL RETRY (ATTEMPT 3)
      # ============================================
      - name: 🛠️ Deep Fix & Final Retry (Attempt 3)
        if: failure()
        run: |
          echo "=========================================="
          echo "=== DEEP FIX & FINAL RETRY ==="
          echo "=========================================="
          
          BUILD_TYPE="${{ steps.build_info.outputs.build_type }}"
          echo "Build type: $BUILD_TYPE"
          
          if ! command -v flutter >/dev/null 2>&1; then
            echo "⚠️ Flutter tidak ditemukan! Mencoba re-setup..."
            
            FLUTTER_CACHE="/opt/hostedtoolcache/flutter"
            if [ -d "$FLUTTER_CACHE" ]; then
              FLUTTER_DIR=$(find "$FLUTTER_CACHE" -maxdepth 2 -name "flutter" -type f 2>/dev/null | head -1)
              if [ -n "$FLUTTER_DIR" ]; then
                echo "✅ Flutter ditemukan di cache: $FLUTTER_DIR"
                export PATH="$(dirname "$FLUTTER_DIR"):$PATH"
                flutter --version
              fi
            fi
            
            if ! command -v flutter >/dev/null 2>&1; then
              echo "📦 Download Flutter SDK..."
              FLUTTER_VERSION="3.32.8"
              curl -L -o flutter.tar.xz "https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_${FLUTTER_VERSION}-stable.tar.xz"
              tar -xf flutter.tar.xz -C /tmp/
              export PATH="/tmp/flutter/bin:$PATH"
              flutter config --no-analytics
              flutter --version
              echo "✅ Flutter SDK siap!"
            fi
          fi
          
          if ! command -v flutter >/dev/null 2>&1; then
            echo "❌ Flutter tetap tidak ditemukan!"
            exit 1
          fi
          
          echo "Flutter: $(flutter --version)"
          
          flutter clean 2>/dev/null || true
          rm -rf build/
          if [ -d "android" ]; then
            rm -rf android/.gradle/
            rm -rf android/app/build/
            
            find android -name "*.gradle" -o -name "*.gradle.kts" | while read f; do
              sed -i 's/compileSdkVersion.*$/compileSdkVersion 36/g' "$f"
              sed -i 's/minSdkVersion.*$/minSdkVersion 23/g' "$f"
              sed -i 's/targetSdkVersion.*$/targetSdkVersion 36/g' "$f"
            done
            
            find android -name "build.gradle" | while read f; do
              if ! grep -q "lintOptions" "$f"; then
                sed -i '/android {/a \    lintOptions {\n        checkReleaseBuilds false\n        abortOnError false\n    }' "$f"
              fi
            done
          fi
          
          flutter pub get
          
          echo "🔄 Final retry..."
          if [ "$BUILD_TYPE" = "apk-debug" ]; then
            flutter build apk --debug --no-shrink 2>&1 | tee build_log.txt
          elif [ "$BUILD_TYPE" = "apk-release" ]; then
            flutter build apk --release 2>&1 | tee build_log.txt
          elif [ "$BUILD_TYPE" = "aab-release" ]; then
            flutter build appbundle --release 2>&1 | tee build_log.txt
          fi
          
          echo ""
          echo "=== HASIL BUILD FINAL ==="
          find . -name "*.apk" -type f 2>/dev/null
          find . -name "*.aab" -type f 2>/dev/null

      # ============================================
      # 📦 PREPARE OUTPUT FILES (AGRESIF)
      # ============================================
      - name: 📦 Prepare Output Files
        if: always()
        run: |
          set +e
          
          echo "=========================================="
          echo "=== PREPARING OUTPUT FILES ==="
          echo "=========================================="
          
          BUILD_TYPE="${{ steps.build_info.outputs.build_type }}"
          if [ -z "$BUILD_TYPE" ]; then
            BUILD_TYPE="apk-release"
          fi
          
          TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
          APP_NAME=$(grep '^name:' pubspec.yaml 2>/dev/null | awk '{print $2}' | tr -d '\r' || echo "app")
          
          echo "Build type: $BUILD_TYPE"
          echo "APP_NAME: $APP_NAME"
          echo "PWD: $(pwd)"
          echo ""
          
          # Cek isi seluruh workspace
          echo "=== ISI WORKSPACE ==="
          ls -la
          echo ""
          echo "=== ISI build/ ==="
          ls -laR build/ 2>/dev/null | head -100
          echo ""
          
          mkdir -p output
          
          # Cari dengan berbagai cara
          echo "=== MENCARI APK/AAB ==="
          
          # Cara 1: find di current directory
          find . -name "*.apk" -type f 2>/dev/null | while read f; do
            echo "FOUND APK: $f ($(du -h "$f" 2>/dev/null | cut -f1))"
            cp "$f" output/
          done
          
          find . -name "*.aab" -type f 2>/dev/null | while read f; do
            echo "FOUND AAB: $f ($(du -h "$f" 2>/dev/null | cut -f1))"
            cp "$f" output/
          done
          
          # Cara 2: find di home directory
          find /home/runner -name "*.apk" -type f 2>/dev/null | while read f; do
            echo "FOUND APK (home): $f"
            cp "$f" output/
          done
          
          find /home/runner -name "*.aab" -type f 2>/dev/null | while read f; do
            echo "FOUND AAB (home): $f"
            cp "$f" output/
          done
          
          # Cara 3: Cek lokasi spesifik
          for LOCATION in \
            "build/app/outputs/flutter-apk" \
            "build/app/outputs/bundle/release" \
            "build/app/outputs/apk/release" \
            "build/app/outputs/apk/debug" \
            "build/app/outputs/apk" \
            "build/app/outputs/bundle"; do
            if [ -d "$LOCATION" ]; then
              echo "Checking: $LOCATION"
              ls -lh "$LOCATION/" 2>/dev/null
              cp "$LOCATION"/*.apk output/ 2>/dev/null
              cp "$LOCATION"/*.aab output/ 2>/dev/null
            fi
          done
          
          echo ""
          echo "=== FINAL OUTPUT ==="
          ls -lh output/ 2>/dev/null || echo "(kosong)"
          
          # Buat dummy file untuk memastikan artifact selalu ada
          if [ -z "$(ls -A output/ 2>/dev/null)" ]; then
            echo "⚠️ TIDAK ADA FILE! Membuat build log sebagai artifact..."
            cp build_log.txt output/ 2>/dev/null || echo "No build log" > output/build_log.txt
            echo "Build Type: $BUILD_TYPE" > output/build_info.txt
            echo "Timestamp: $TIMESTAMP" >> output/build_info.txt
            echo "Status: No APK/AAB found" >> output/build_info.txt
            find . -name "*.apk" -o -name "*.aab" 2>/dev/null >> output/build_info.txt
          fi
          
          echo ""
          echo "=== ISI OUTPUT AKHIR ==="
          ls -lh output/ 2>/dev/null
          
          echo ""
          echo "=== PREPARE OUTPUT SELESAI ==="
          exit 0

      # ============================================
      # 📤 UPLOAD BUILD ARTIFACT
      # ============================================
      - name: 📤 Upload Build Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-build-${{ steps.build_info.outputs.build_type }}-${{ github.run_number }}
          path: |
            output/
            build/app/outputs/flutter-apk/
            build/app/outputs/bundle/release/
            build/app/outputs/apk/release/
            build/app/outputs/apk/debug/
          retention-days: 3
          if-no-files-found: warn
          overwrite: true

      - name: Build Summary
        if: always()
        run: |
          echo "## 🚀 Flutter Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Key | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Build Type | ${{ steps.build_info.outputs.build_type }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Flutter Version | ${{ github.event.inputs.flutter_version != '' && github.event.inputs.flutter_version || '3.32.8 (default)' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Run Number | #${{ github.run_number }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Triggered By | ${{ github.event_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Commit | ${{ github.sha }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build 1 | ${{ steps.build_debug.outcome || steps.build_release.outcome || steps.build_aab.outcome }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Output Files" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "output/:" >> $GITHUB_STEP_SUMMARY
          ls -lh output/ 2>/dev/null >> $GITHUB_STEP_SUMMARY || echo "(empty)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "build/app/outputs/flutter-apk/:" >> $GITHUB_STEP_SUMMARY
          ls -lh build/app/outputs/flutter-apk/ 2>/dev/null >> $GITHUB_STEP_SUMMARY || echo "(empty)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "build/app/outputs/bundle/release/:" >> $GITHUB_STEP_SUMMARY
          ls -lh build/app/outputs/bundle/release/ 2>/dev/null >> $GITHUB_STEP_SUMMARY || echo "(empty)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Semua file APK/AAB ditemukan:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          find . -name "*.apk" -o -name "*.aab" 2>/dev/null | head -20 >> $GITHUB_STEP_SUMMARY || echo "(none)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
